# ユーザーライブラリ(手元のコンテンツ)はコミットしない
/library/

# デモライブラリへの実行時書き込み(コレクション操作・E2Eテストで生成される)
/demo/library/collections.json

# レビュー報告bundle(参照アプリのスクショ・名称を含むローカル資料。公開リポジトリに含めない)
/reviews/
/scratchpad/

# 禁止語設定はリポジトリの外(KIKIMIRU_GATE_CONFIG / OS既定のユーザ設定ディレクトリ)に置く。
# ハッシュ化しても、正規化関数が公開されている以上「当てた語をタダで照合できる」
# オラクルになるため、公開物に含めてはならない。誤って戻された場合の保険として除外する
release_gate_config.json

__pycache__/
*.pyc
.DS_Store
Thumbs.db
